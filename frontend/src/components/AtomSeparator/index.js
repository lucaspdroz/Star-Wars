import './style.scss'

const AtomSeparator = ({ text, size }) => {
    return (
        <div className="title-card">
            {text} {size}
        </div>
    );
}

export default AtomSeparator;
